# Basic Auth pro middleware.ts (ochrana celé appky na Vercel edge).
# Nastav skutečné hodnoty jako Environment Variables ve Vercel projektu
# (Production i Preview), lokálně zkopíruj do .env a spusť přes `vercel dev`.
BASIC_AUTH_USER=
BASIC_AUTH_PASSWORD=
